---
- hosts: app
  become: yes
  tasks:
    - name: Common packages
      ansible.builtin.package:
        state: present
        name:
          - git
          - wget
          - unzip
          - vim
